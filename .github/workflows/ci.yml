name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install runtime deps for the pure-logic tests
        # The smoke tests import card_renderer/themer/cc_frames (no Ollama/ComfyUI).
        run: python -m pip install --upgrade pip pillow requests pixie-python

      - name: Syntax-check all tracked Python
        run: python -m py_compile $(git ls-files '*.py')

      - name: Smoke tests
        run: python tests/test_smoke.py

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install + build frontend
        working-directory: frontend
        run: |
          npm ci || npm install
          npm run build
