# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Generated output — large and build-specific
renders/
generated_art/
scryfall_cache/
test_cards/
test_output/
*.log
*.log.err
*.err

# Frontend build output (rebuilt with npm run build)
frontend/dist/
frontend/node_modules/

# Misc
*.png
test_*.py
!tests/             # but DO track the committed test suite under tests/
!tests/**
!docs/gallery/      # and the README gallery images (built-in frames only)
!docs/gallery/**
test_*.png
CLAUDE.md

# Session / internal dev docs (not shipped)
SESSION_*.md
*_COMPLETE.md
DEPLOYMENT_*.md
FINAL_*.md
FIXES_*.md
REVIEW_*.md
SANITY_*.md
LORA_*.md
CODE_REVIEW.md
BATCH_SIZE_TESTING_PLAN.md
OPTIMIZATION_RESEARCH*.md

# Stray bat-redirect output (from non-interactive launches)
bat_stdout.txt
bat_stderr.txt

# Local marketing/launch assets (not part of the app)
reddit_assets/

# Card Conjurer frame assets are user-supplied & copyrighted — never commit them.
# Point MYTHFORGE_CC_DIR at a CC install OUTSIDE this repo; this guard only
# catches the case where someone clones/copies CC into the repo by mistake.
cardconjurer/
cardconjurer-*/
cc_assets*/
**/img/frames/m15/
# Machine-specific Card Conjurer folder setting (set via the in-app field)
cc_config.json

# Face upload staging
face_uploads/

# Imported-deck + Scryfall card caches (regenerable)
cache/
ro_showcase/
